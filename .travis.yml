language: python
python:
    - "2.6"
    - "2.7"

install:
    - pip install -r dev-requirements.txt
    - pip install -r requirements.txt

before_script:
    - make

script:
    - tox
    - pyflakes parsefin
